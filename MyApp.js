// var express = require('express');
// var app = express();

// absolutePath = __dirname + "/views/index.html"
// otherPath = __dirname + "/public"


// app.use("/", express.static("/"))
// app.use("/public", express.static(otherPath))
// app.use("/", bodyParser.urlencoded({extended: false}))
